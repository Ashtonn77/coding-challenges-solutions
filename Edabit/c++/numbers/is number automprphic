one -->

bool isAutomorphic(int n) {
	long long nsqr = (long long)n * (long long)n;
  std::string s = std::to_string(n);
  std::string ns = std::to_string(nsqr);
  return ns.substr(ns.length() - s.length()) == s;
}

------------------------------------
