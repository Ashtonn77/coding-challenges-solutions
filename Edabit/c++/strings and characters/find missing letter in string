one -->

std::string missingLetter(std::string str) {
	for(int i = 0; i < str.length()-1; i++){
    if((int)str[i + 1] != (int)str[i] + 1)
    return str = (char)(str[i] +1);
  }
	return "No Missing Letter";
}

-------------------
